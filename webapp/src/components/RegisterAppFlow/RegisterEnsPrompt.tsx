"use client"
import { FC } from "react";

const RegisterEnsPrompt: FC = () => {
  return (
    <div>
      This domain is not registered, yet. You can claim it on ens.domains.
    </div>
  );
}

export default RegisterEnsPrompt;
